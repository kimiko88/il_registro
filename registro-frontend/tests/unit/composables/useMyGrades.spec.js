import { describe, it, expect } from 'vitest'
import { useMyGrades } from '@/composables/useMyGrades'

describe('useMyGrades', () => {
    it('calculates averages', () => {
        // Mock data
        const grades = [{ value: 6 }, { value: 8 }]
        // const { calculateAverage } = useMyGrades()
        // expect(calculateAverage(grades)).toBe(7)
    })
})
