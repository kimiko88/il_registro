name: Health Check

on:
  schedule:
    - cron: '0 0 * * *'  # Once a day (midnight UTC)


jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check API health
        run: |
          echo "WIP - Monitoring setup is pending"
          # curl -f https://api.registro.it/health || exit 1
          # curl -f https://api.registro.it/ready || exit 1
      # - name: Check Frontend
      #   run: curl -f https://registro.it/ || exit 1
      # - name: Notify Slack on failure
        # if: failure()
        # uses: slackapi/slack-github-action@v1
        # with:
        #   webhook-url: ${{ secrets.SLACK_WEBHOOK }}
