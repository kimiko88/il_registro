name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Build Docker Image
      run: docker build . --file docker/Dockerfile.prod --tag my-registry/registro-backend:latest
      
    - name: Push (Placeholder)
      run: echo "Pushing to registry..."
